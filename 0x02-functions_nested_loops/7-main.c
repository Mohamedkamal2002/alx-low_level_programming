#include "main.h"


int test_last(int i)
{
	return (print_last_digit(i));
}


/**
 * main - call the function
 *
 * Description: ~
 * Return: always 0
 */

int main(void)
{
	int r;

	r = test_last(98);
	return (0);
}
